#!/usr/bin/env python3
# hours_to_seconds.py

# Define the variable as specified
hours = 2

# Calculate seconds using the conversion factor (1 hour = 3600 seconds)
seconds = hours * 3600

# Print the result in the required format
print(f"{hours} hour(s) is {seconds} seconds.")
